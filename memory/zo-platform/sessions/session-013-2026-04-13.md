# Session Summary: 2026-04-13 (prod-001 readiness)
**Date**: 2026-04-13
**Duration**: ~3 hours
**Mode**: maintain
**Agent**: orchestrator (zo-dev)
**Branch**: claude/confident-wescoff (worktree)

## Trigger

User finished CIFAR-10 demo and wanted to start prod-001 (first production project). Asked: "Is anything in the repo that needs to be updated for it to be ready? What should I do to set up prod-001? It's already a git repo on my branch: samtukra."

## Accomplished

### Conversational `zo init` via Init Architect (20th agent)

- New agent `.claude/agents/init-architect.md` (Opus, team: init) — interview protocol, inspect-then-write pattern, ~25 enumerated failure modes across 7 categories
- `zo init project` now defaults to launching tmux with the architect; the architect calls `zo init project --no-tmux ...` to commit writes
- Headless flags: `--no-tmux`, `--branch`, `--scaffold-delivery`, `--existing-repo`, `--base-image`, `--gpu-host`, `--data-path`, `--no-detect`, `--layout-mode`, `--dry-run`, `--reset`, `--yes`
- Guardrails at CLI boundary: tmux availability check, mutually-exclusive flag detection, `--existing-repo` must be a git dir, `--layout-mode=adaptive` requires `--existing-repo`, `--dry-run` requires `--no-tmux`, branch existence warning via `git rev-parse`

### `src/zo/environment.py` — host detection

- `EnvironmentInfo` dataclass: platform, python_version, docker_available, docker_compose_available, gpu_count, gpu_names, gpu_memory_gb, cuda_version, nvidia_driver_version
- `detect_environment()` — best-effort, all probes wrapped in try/except, safe defaults on failure
- `suggest_base_image(env)` — heuristic PyTorch image picker (CUDA 12.4 / 12.1 / 11.8 + safe fallback + CPU)
- 10 unit tests covering happy path, missing nvidia-smi, errored nvidia-smi, missing Docker, all CUDA branches

### Scaffold `layout_mode={standard,adaptive}` — adapts to existing code layouts

- Split `_DIRECTORIES` into `_META_DIRECTORIES` (always created: configs/, experiments/, reports/, notebooks/phase/, docker/) and `_STANDARD_DIRECTORIES` (only standard mode: src/*, data/*, models/, human notebooks, tests/*)
- Adaptive mode also skips README.md, pyproject.toml, .gitignore template files (existing repos already have these)
- `.gitkeep` only written into truly empty directories — no pollution of pre-existing code dirs in overlay
- Adaptive vs standard logged with explicit "added | preserved" counts

### Plan template `## Environment` section + auto-detect

- Three blocks: **Host** (where ZO runs), **Training target** (where Docker runs), **Data** (layout, path, docker mounts)
- `_render_plan_template` interpolates detected values; user flags override; `--no-detect` keeps placeholders
- Resolves known issue #6 (Plan.md missing Environment section)

### Target template — branch + responsibility-based agent_working_dirs

- `{target_branch}` placeholder so `--branch samtukra` lands in target.target.md
- `agent_working_dirs` updated from flat (`data/`, `models/`) to responsibility-based (`src/data/`, `src/model/`, `src/engineering/`, `src/inference/`) matching new scaffold layout
- Added xai-agent + domain-evaluator → reports/ mapping

### `--dry-run` and `--reset` for mid-flight adaptability

- `--dry-run`: prints the file tree, target.md content, and plan.md Environment section that WOULD be written, with no filesystem mutations. Init Architect runs this before every commit.
- `--reset`: deletes `memory/{project}/`, `targets/{project}.target.md`, `plans/{project}.md`. NEVER touches delivery repo. Confirms by asking the user to type the project name (or `--yes` to skip).
- Init Architect protocol updated: dry-run mandatory before commit, partial-match guidance (default standard for partial src/), semantic-alias guidance (adaptive + map for `src/data_loading` → `src/data`), `--reset` as canonical rollback path

### Tests

- 13 new headless-flag tests (test_cli.py — branch flag, existing-repo validation, mutually-exclusive flags, adaptive mode, .gitkeep behavior, environment population, gpu-host/data-path in plan, tmux guardrail, conversational error)
- 3 new prompt construction tests (test_cli.py — project name, hints, no-direct-writes reinforcement)
- 3 new dry-run tests (test_cli.py — no writes, branch+layout shown, rejected without --no-tmux)
- 6 new reset tests (test_cli.py — deletes artifacts, preserves delivery + user code, no-op on nonexistent, refuses on name mismatch, accepts matching name)
- 6 new scaffold tests (test_cli.py — overlay logging, gitkeep in empty dirs only, adaptive skips src/data, adaptive omits README/pyproject/.gitignore, invalid mode rejection)
- 10 new env detection tests (test_environment.py)
- 2 new integration tests (test_init_lifecycle.py — full lifecycle dry-run → commit → dry-run → reset → reinit; dry-run/commit dir match)
- **Total: 451 passing, 7 skipped** (was 415)

## Doc cascades

- README.md: tests badge 382→439, agents 19→20, agent count cascades in roster table + doc string + footer summary
- setup.sh: EXPECTED_AGENTS adds init-architect, count 19→20 in pass/fail messages
- specs/agents.md: Team Philosophy now lists 4 teams (project delivery 11, platform build 6, draft scouts 2, init 1) totalling 20
- .claude/agents/lead-orchestrator.md: 19→20 pre-defined agents
- plans/zero-operators-build.md: 19→20 agent count throughout
- docs/COMMANDS.md: full new init flag surface documented
- docs/SAMPLE_PROJECT.md: conversational-by-default + headless example + reset
- docs/DELIVERY_STRUCTURE.md: existing-repo/adaptive note added to opening
- docs/demo.html: sample project section + delivery structure intro updated
- memory/zo-platform/STATE.md: position, completed list, what's next, session metadata
- memory/zo-platform/DECISION_LOG.md: 4 new entries (conversational init, adaptive layout mode, plan Environment, dry-run + reset)
- memory/zo-platform/PRIORS.md: PR-017 (conversational > flag proliferation), PR-018 (adaptive layout preserves existing code), PR-019 (preview + reversal for conversational commands)

## Decisions made

1. Conversational `zo init` follows the proven `zo draft` pattern (tmux + agent + `_launch_and_monitor`)
2. Agent never writes files — routes ALL writes through `zo init --no-tmux ...` (single source of truth)
3. Layout mode `adaptive` preserves user `src/` layout; agent customises STRUCTURE.md + agent_working_dirs to point at real paths (the one allowed direct-write exception, narrowly scoped)
4. `--dry-run` mandatory before commit; `--reset` for clean rollback that never touches delivery repo
5. Confirmation by typing project name (not Y/N) — forces attention on which project is being wiped

## Blockers hit

- ruff E501 on a long comment in environment.py — fixed by rewriting the comment
- ruff F401 on unused pytest import in test_environment.py — removed
- Existing test_cli.py line 28 still over 100 chars (pre-existing, not mine, ruff config skips tests/)
- Hook reminders firing on every cli.py edit asking for cascade — addressed in batch at the end

## What's next

1. **prod-001 setup** — run `zo init prod-001` from main repo (not worktree). Agent will inspect, ask 6 questions, dry-run, commit.
2. Phase completion snapshots (C1) — capture context at phase boundaries
3. Domain evaluator refactor — project-specific via plan domain priors
4. XAI + Domain Evaluator activation for prod-001 Phase 5
5. Remote-data manifest support for `zo draft` (Data Scout reads YAML manifest for GPU-server data)

## Session metadata

last_checkpoint: 2026-04-13T12:30:00Z
test_count: 453 passed, 7 skipped
lint: ruff clean (src/zo/)
validation: scripts/validate-docs.sh 10/10 passed, 0 warnings
new_files: src/zo/environment.py, .claude/agents/init-architect.md, tests/unit/test_environment.py, tests/integration/test_init_lifecycle.py, memory/zo-platform/sessions/session-013-2026-04-13.md
modified: src/zo/cli.py, src/zo/scaffold.py, .claude/agents/lead-orchestrator.md, README.md, setup.sh, specs/agents.md, plans/zero-operators-build.md, docs/COMMANDS.md, docs/SAMPLE_PROJECT.md, docs/DELIVERY_STRUCTURE.md, docs/demo.html, tests/unit/test_cli.py, memory/zo-platform/STATE.md, memory/zo-platform/DECISION_LOG.md, memory/zo-platform/PRIORS.md
priors_added: PR-017, PR-018, PR-019
