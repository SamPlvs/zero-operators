# Session 034 — 2026-05-30

## Focus
Batch B+C (training intelligence) — first of the process-hardening batches from the user's request to stop manually re-instructing ZO every run. Enforcement-over-aspiration (PR-005) applied to the autonomous Phase-4 run.

## Shipped
- **training-checker agent** (`.claude/agents/training-checker.md`, Sonnet, phase-in, project): per-model-run live monitor (`training-{modelname}-checker`) — tails `metrics.jsonl`/`training_status.json`, alerts on NaN/divergence/overfit/stall, writes `diagnosis.md` + feeds `next.md`. Enforced via always-fires Phase-4 instruction in `Orchestrator._prompt_experiment_context` + `AGENT_PHASE_MAP` + role/ownership/off-limits maps.
- **should_checkpoint helper** (`src/zo/training_metrics.py`) + `model-builder.md` "Checkpointing and Disaster Recovery (REQUIRED)" section (DL every-10-epochs + best + last resumable; ML per-fold + best + HPO-state).
- **research-scout general-AI track** (`research-scout.md`, Phase-4 iteration, method-first, pairs with checker).
- **experiment checklist** (`experiments.render_checklist`/`write_checklist`) auto-refreshed on every registry mutation → `.zo/experiments/CHECKLIST.md`.
- Agent roster 20 → 21; full doc cascade; +20 tests (760 → 780).

## Verification
- pytest 780 passed / 7 skipped on Python 3.11 AND 3.12.
- ruff `src/` clean; test additions ruff-clean (2 pre-existing warnings in `test_training_metrics.py` left out of scope per PR-039).
- validate-docs 9 pass / 0 fail / 2 warn (client-blocklist skip + grep-768-vs-pytest-780 badge gap).

## Memory
- DECISION_LOG: 2026-05-30 FEATURE entry.
- PRIORS: PR-040 — training-time observability + checkpoint cadence must be enforced, not aspirational.

## Next
Batch A (self-evolution: seed → load → write → automated sanitized promote), then D (optimization audit + `software-engineer` agent), then E (swarm reinforcement + idle-agent shutdown). Standing Tier-1 roadmap: caveman ablation, onboarding hardening.

## PR
Branch `claude/training-intelligence` → PR against `main`.
